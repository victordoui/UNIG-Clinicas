import { Send, CheckCircle2 } from 'lucide-react';
import { MainLayout } from '@/components/layout/MainLayout';
import { useCanReadAcademic } from '@/components/academico/StaffOnly';
import { usePublishedAcademicSchedules, useAcademicMeetings } from '@/hooks/useAcademicSchedules';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function Publicacao() { const canRead = useCanReadAcademic(); const { data: schedules = [] } = usePublishedAcademicSchedules(); const { data: meetings = [] } = useAcademicMeetings(); if (!canRead) return <MainLayout><p className="text-sm text-muted-foreground">Sem permissão.</p></MainLayout>; return <MainLayout><div className="space-y-6"><div><h1 className="flex items-center gap-2 text-2xl font-bold"><Send className="h-6 w-6 text-primary" />Publicação</h1><p className="mt-1 text-sm text-muted-foreground">Grades publicadas ficam disponíveis para consulta em todo o sistema.</p></div><Card className="border-emerald-200 bg-emerald-50/50"><CardContent className="flex items-center gap-3 p-5"><CheckCircle2 className="h-7 w-7 text-emerald-600" /><div><p className="font-semibold text-emerald-900">{schedules.length} grade(s) publicada(s)</p><p className="text-sm text-emerald-800">{meetings.length} aula(s) vinculada(s) estão disponíveis para consulta.</p></div></CardContent></Card><div className="grid gap-4 md:grid-cols-2">{schedules.map((schedule) => <Card key={schedule.classCode}><CardHeader><CardTitle className="text-base">{schedule.course}</CardTitle></CardHeader><CardContent className="space-y-1 text-sm text-muted-foreground"><p className="font-medium text-foreground">{schedule.classCode} · {schedule.semester}</p><p>Período {schedule.academicPeriod}</p><p>{schedule.entries.length} encontro(s) publicado(s)</p></CardContent></Card>)}</div></div></MainLayout>; }
