import { CalendarRange } from 'lucide-react';
import { MainLayout } from '@/components/layout/MainLayout';
import { useCanReadAcademic } from '@/components/academico/StaffOnly';
import { useAcademicEvents } from '@/hooks/useAcademicEvents';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';

export default function Calendario() { const canRead = useCanReadAcademic(); const { data: events = [], isLoading } = useAcademicEvents(); if (!canRead) return <MainLayout><p className="text-sm text-muted-foreground">Sem permissão.</p></MainLayout>; return <MainLayout><div className="space-y-6"><div><h1 className="flex items-center gap-2 text-2xl font-bold"><CalendarRange className="h-6 w-6 text-primary" />Calendário Acadêmico</h1><p className="mt-1 text-sm text-muted-foreground">Eventos, prazos e marcos que impactam a organização acadêmica.</p></div><Card><CardHeader><CardTitle className="text-base">Próximos eventos</CardTitle></CardHeader><CardContent>{isLoading ? <p className="text-sm text-muted-foreground">Carregando calendário…</p> : !events.length ? <p className="py-8 text-center text-sm text-muted-foreground">Nenhum evento acadêmico cadastrado.</p> : <div className="space-y-3">{events.map((event) => <div key={event.id} className="flex items-center gap-4 rounded-lg border p-4"><div className="w-14 rounded-md bg-primary/10 p-2 text-center text-primary"><p className="text-xs font-bold">{new Date(event.starts_at).toLocaleDateString('pt-BR', { month: 'short' }).replace('.', '').toUpperCase()}</p><p className="text-xl font-bold">{new Date(event.starts_at).getDate()}</p></div><div className="min-w-0 flex-1"><p className="font-medium">{event.title}</p><p className="text-sm text-muted-foreground">{new Date(event.starts_at).toLocaleString('pt-BR')}</p></div><Badge variant="secondary">{event.event_type}</Badge></div>)}</div>}</CardContent></Card></div></MainLayout>; }
